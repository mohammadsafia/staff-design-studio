import './style.css';
import './landing.css';
import './invite/style.css';
import { createInviteSession } from './invite/model.js';
import { mountInvite } from './invite/view.js';
import { evidenceFor } from './evidence.js';
import { landings, landingView, bindLanding } from './landings.js';
import { cases, refUrl } from './cases.js';
import { screen } from './screens.js';

const app = document.querySelector('#app');
const skillUrl = `${import.meta.env.BASE_URL}skills/craft-product-ui/SKILL.md`;
const stateByCase = Object.fromEntries(cases.map(item => [item.id, item.initial]));
let mode = 'split';
let width = 'desktop';
let split = 50;
let inviteSession;
let unmountInvite;
function detachInvite() { unmountInvite?.(); unmountInvite = undefined; }
function resetInvite() { detachInvite(); inviteSession?.dispose(); inviteSession = undefined; }
function connectInvite(scenario) {
  inviteSession ??= createInviteSession({ scenario });
  unmountInvite = mountInvite(app.querySelector('.invite-host'), inviteSession);
  void inviteSession.startScenario();
}

function currentCase() { return cases.find(item => item.id === location.hash.slice(1)); }
function shell(content) {
  detachInvite();
  app.innerHTML = `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><a class="brand" href="#" aria-label="Staff Design Studio home"><span class="brand-glyph">S<span>✳</span></span><span>STAFF<br>DESIGN STUDIO</span></a><nav aria-label="Main navigation"><a href="#cases">The cases</a><a href="${skillUrl}" target="_blank" rel="noopener">Read the skill <span aria-hidden="true">↗</span></a><a class="repo-link" href="https://github.com/mohammadsafia/staff-design-studio" target="_blank" rel="noopener">GitHub <span aria-hidden="true">↗</span></a></nav></header>${content}<footer class="site-footer"><span>STAFF DESIGN STUDIO <i>✳</i> 2026</span><span>Constructed scenarios. Observable decisions. No invented scores.</span><a href="${skillUrl}">Skill source ↗</a></footer>`;
}
function home() {
  document.title = 'Staff Design Studio — Proof in the interface';
  shell(`<main id="main"><section class="hero"><div class="hero-topline"><span><i class="live-dot"></i> THE INTERACTIVE SHOWCASE</span><span>VOL. 01 — SIX PRODUCT JOURNEYS + THREE LANDINGS</span></div><div class="hero-grid"><div><p class="eyebrow">CRAFT PRODUCT UI / FIELD NOTES</p><h1>Design judgment<br>your coding agent<br><em>can prove.</em></h1><p class="hero-intro">From a flawed first pass to a working interface. Scrub the changes, test the states, and see which decisions hold up when the screen gets difficult.</p><a class="hero-cta" href="#cases">Explore the six cases <span aria-hidden="true">↘</span></a></div><div class="hero-graphic" aria-hidden="true"><div class="graphic-grid"></div><div class="graphic-slab one"><span>BEFORE</span><b>?</b><small>Assumption</small></div><div class="graphic-slab two"><span>AFTER</span><b>✓</b><small>Inspect the evidence</small></div><div class="graphic-caption">DESIGN / OBSERVE / REVISE</div></div></div><div class="hero-bottom"><span>01—06 &nbsp; CASE STUDIES</span><span>SCROLL TO INSPECT ↓</span><span>BUILT WITH CRAFT-PRODUCT-UI</span></div></section><section class="case-index" id="cases" aria-labelledby="cases-heading"><div class="section-heading"><div><p class="eyebrow">THE WORKBENCH</p><h2 id="cases-heading">Six places where<br><em>details matter.</em></h2></div><p>Every before screen is a constructed design problem. Every after screen is an interactive prototype, with its assumptions, states and checks exposed.</p></div><div class="case-grid">${cases.map(c => `<a class="case-card" href="#${c.id}"><div class="case-card-top"><span>${c.number} / 06</span><span aria-hidden="true">↗</span></div><div class="case-card-art art-${c.number}"><span class="art-frame"><span class="art-line"></span><span class="art-line short"></span><span class="art-block"></span><span class="art-line"></span></span><span class="art-index">${c.number}</span></div><p class="case-category">${c.category}</p><h3>${c.title}</h3><p>${c.summary}</p><span class="case-card-action">Open case <span aria-hidden="true">↗</span></span></a>`).join('')}</div></section><section class="landing-index" id="landings" aria-labelledby="landings-heading"><div class="section-heading"><div><p class="eyebrow">THREE MORE DIRECTIONS</p><h2 id="landings-heading">Full pages, not<br><em>just a hero.</em></h2></div><p>Distinct landing pages with live local interactions. Explore each concept at its own pace and viewport width.</p></div><div class="landing-grid">${landings.map(l => `<a class="landing-card" href="?landing=${l.id}"><div class="landing-card-art"><span lang="${l.id === 'landing-makan' ? 'ar' : 'en'}">${l.title}</span></div><small>${l.type.toUpperCase()} / ${l.palette.toUpperCase()}</small><h3>${l.title}</h3><p>${l.description}</p><span>Visit landing page <span aria-hidden="true">↗</span></span></a>`).join('')}</div></section><section class="method"><p class="eyebrow">METHOD / NOT MAGIC</p><div><h2>Make the work<br><em>inspectable.</em></h2><p>These are deterministic demonstrations, not customer studies or measured performance claims. The skill guides the decisions; the workbench shows the states and the check log shows what was actually exercised.</p><a href="${skillUrl}">Read craft-product-ui ↗</a></div><div class="method-steps"><div><b>01</b><span>Identify the task and failure</span></div><div><b>02</b><span>Design the recovery path</span></div><div><b>03</b><span>Build and test the states</span></div></div></section></main>`);
}
function detail(c, focusSelector) {
  const state = stateByCase[c.id];
  document.title = `${c.title} — Staff Design Studio`;
  shell(`<main id="main" class="detail-main"><div class="detail-nav"><a href="#cases">← All cases</a><span>CASE ${c.number} / 06</span><div><a href="#${cases[(Number(c.number) + 4) % 6].id}">Previous</a><span>/</span><a href="#${cases[Number(c.number) % 6].id}">Next →</a></div></div><section class="case-hero"><p class="eyebrow">${c.category}</p><h1>${c.title}<span class="case-asterisk">✳</span></h1><p>${c.summary}</p><div class="case-context"><span>THE SCENARIO</span><p>${c.context}</p><span>SIMULATED PROTOTYPE · NO LIVE DATA OR TRANSACTIONS</span></div></section><p class="benchmark-scope">${c.id === 'invite-team' ? 'Executable Invite benchmark: local service only. Browser layout, keyboard navigation and screenshots remain unverified. <a href="?benchmark=invite-team">Open at the real viewport width ↗</a>' : 'Presentation draft. This scenario predates the canonical benchmark; it does not yet satisfy its product rules or workflow coverage.'}</p><section class="workbench" aria-labelledby="workbench-heading"><div class="workbench-title"><div><p class="eyebrow">INTERACTIVE WORKBENCH / ${c.number}</p><h2 id="workbench-heading">Inspect the difference.</h2></div><p>Drag to compare. Switch states and viewport sizes to see the workflow beyond the ideal screen.</p></div><div class="workbench-controls"><div class="control-group" role="group" aria-label="Comparison view"><span>VIEW</span>${['before','split','after'].map(m => `<button type="button" data-mode="${m}" class="${mode === m ? 'active' : ''}" aria-pressed="${mode === m}">${m === 'split' ? 'Compare' : m[0].toUpperCase()+m.slice(1)}</button>`).join('')}</div><div class="control-group" role="group" aria-label="Preview width"><span>PREVIEW</span>${['mobile','tablet','desktop'].map(w => `<button type="button" data-width="${w}" class="${width === w ? 'active' : ''}" aria-pressed="${width === w}">${w[0].toUpperCase()+w.slice(1)}</button>`).join('')}</div></div><div class="state-strip"><span>TEST FIXTURE · RESETS LOCAL DATA</span><div role="group" aria-label="Scenario state">${c.states.map(s => `<button type="button" data-state="${s}" class="${state === s ? 'active' : ''}" aria-pressed="${state === s}">${s[0].toUpperCase()+s.slice(1)}</button>`).join('')}</div></div><div class="stage"><div class="stage-heading"><span>${mode === 'before' ? 'BEFORE / INTENTIONALLY FLAWED' : mode === 'after' ? 'AFTER / INTERACTIVE PROTOTYPE' : 'BEFORE ↔ AFTER / DRAG TO REVEAL'}</span><span>${width.toUpperCase()} · ${width === 'mobile' ? '390' : width === 'tablet' ? '820' : '1280'} PX CANVAS</span></div><div class="stage-scroll"><div class="preview-frame ${width} ${mode}" style="--split:${split}%"><div class="preview-layer before-layer" aria-hidden="${mode !== 'before'}" ${mode !== 'before' ? 'inert' : ''}>${screen(c.id,state,true)}</div><div class="preview-layer after-layer" aria-hidden="${mode !== 'after'}" ${mode !== 'after' ? 'inert' : ''}>${screen(c.id,state)}</div>${mode === 'split' ? `<div class="split-line" aria-hidden="true"><span>↔</span></div>` : ''}</div></div>${mode === 'split' ? `<label class="scrub-control"><span>BEFORE</span><input type="range" min="0" max="100" value="${split}" aria-label="Reveal after interface" id="split-range"><span>AFTER</span></label>` : `<p class="stage-hint">${mode === 'after' ? 'Use the product controls to try the local workflow.' : 'Intentionally flawed demonstration of the same task.'}</p>`}</div><p class="workbench-disclaimer">Prototype behavior is local to this browser. No account, payment, email, booking or permission change occurs.</p></section><section class="case-analysis"><div class="analysis-heading"><p class="eyebrow">THE DESIGN REVIEW</p><h2>What changed,<br><em>and why.</em></h2></div><div class="findings">${c.findings.map(([n, title, before, after]) => `<article class="finding"><span class="finding-num">${n}</span><div><h3>${title}</h3><div class="finding-columns"><p><small>BEFORE / OBSERVATION</small>${before}</p><p><small>AFTER / DECISION</small>${after}</p></div></div></article>`).join('')}</div></section><section class="case-bottom"><div class="references"><p class="eyebrow">SOURCE / EXACT REFERENCES</p><h2>Guidance used.</h2><p>These files informed the choices in this case. The case itself is a constructed scenario.</p><ul><li><a href="${skillUrl}" target="_blank" rel="noopener">craft-product-ui/SKILL.md <span>↗</span></a></li>${c.refs.map(ref => `<li><a href="${refUrl(ref)}" target="_blank" rel="noopener">references/${ref}.md <span>↗</span></a></li>`).join('')}</ul></div><div class="evidence"><p class="eyebrow">VERIFICATION / ACTUAL CHECKS</p><h2>Evidence log.</h2><p>Source and DOM checks have different limits from browser review. Evidence is tied to the content tested; missing or changed evidence cannot pass.</p>${evidenceFor(c.id)}<a href="https://github.com/mohammadsafia/staff-design-studio#showcase" target="_blank" rel="noopener">Review the documented test log ↗</a></div></section><div class="next-case"><span>CONTINUE THE REVIEW</span><a href="#${cases[Number(c.number) % 6].id}">${cases[Number(c.number) % 6].title} <span aria-hidden="true">↗</span></a></div></main>`);
  if (c.id === 'invite-team') connectInvite(state);
  bindCase(c);
  if (focusSelector) app.querySelector(focusSelector)?.focus();
}
function bindCase(c) {
  app.querySelectorAll('[data-mode]').forEach(btn => btn.addEventListener('click', () => { mode = btn.dataset.mode; detail(c, `[data-mode="${mode}"]`); }));
  app.querySelectorAll('[data-width]').forEach(btn => btn.addEventListener('click', () => { width = btn.dataset.width; detail(c, `[data-width="${width}"]`); }));
  app.querySelectorAll('[data-state]').forEach(btn => btn.addEventListener('click', () => { stateByCase[c.id] = btn.dataset.state; if (c.id === 'invite-team') resetInvite(); detail(c, `[data-state="${stateByCase[c.id]}"]`); }));
  app.querySelectorAll('[data-transition]').forEach(btn => btn.addEventListener('click', () => { stateByCase[c.id] = btn.dataset.transition; detail(c, `[data-state="${stateByCase[c.id]}"]`); document.querySelector('.stage').scrollIntoView({block:'nearest'}); }));
  const range = app.querySelector('#split-range');
  if (range) range.addEventListener('input', () => { split = Number(range.value); app.querySelector('.preview-frame').style.setProperty('--split', `${split}%`); });
}
function standaloneInvite() {
  resetInvite();
  const params = new URLSearchParams(location.search);
  const scenario = cases[0].states.includes(params.get('scenario')) ? params.get('scenario') : 'default';
  document.title = 'Invite benchmark — Staff Design Studio';
  app.innerHTML = `<div class="benchmark-page"><nav class="benchmark-toolbar" aria-label="Benchmark controls"><a href="./#invite-team">← Comparison & evidence</a><span>LOCAL BENCHMARK / REAL VIEWPORT</span><label>Fixture <select id="fixture">${cases[0].states.map(value => `<option value="${value}" ${value === scenario ? 'selected' : ''}>${value}</option>`).join('')}</select></label></nav><div class="invite-host"></div></div>`;
  app.querySelector('#fixture').addEventListener('change', event => { params.set('scenario', event.target.value); location.search = params.toString(); });
  connectInvite(scenario);
}
function render() { if (new URLSearchParams(location.search).get('benchmark') === 'invite-team') { standaloneInvite(); return; } if (currentCase()?.id !== 'invite-team') resetInvite(); const landing = new URLSearchParams(location.search).get('landing'); if (landings.some(item => item.id === landing)) { app.innerHTML = landingView(landing); document.title = `${landings.find(item => item.id === landing).title} — Staff Design Studio`; bindLanding(landing, app); if (location.hash && document.querySelector(location.hash)) document.querySelector(location.hash).scrollIntoView(); else window.scrollTo({top:0,behavior:'instant'}); return; } const c = currentCase(); c ? detail(c) : home(); if (!c && ['#cases','#landings','#main'].includes(location.hash)) document.querySelector(location.hash)?.scrollIntoView(); else window.scrollTo({top:0,behavior:'instant'}); }
window.addEventListener('hashchange', () => { if (new URLSearchParams(location.search).has('landing')) return; render(); });
render();
